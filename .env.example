# 猫域 (NEKO) 环境变量配置示例

# 站点基本信息
SITE_NAME=猫域 (NEKO)
SITE_DESCRIPTION=高端、独立、渐进式 Web 应用 (PWA) 前端

# 后端 API 配置 (仅用于 Docker 部署时的 Nginx 反代参考)
# 注意：前端代码中 API_BASE_URL 默认为 /api/v1，通过 Nginx 转发
BACKEND_URL=https://your-backend-api.com

# PWA 配置
THEME_COLOR=#1a1a1a
BACKGROUND_COLOR=#1a1a1a

# 第三方集成 (可选)
# TAWK_CHAT_ID=your_tawk_id
# GOOGLE_ANALYTICS_ID=your_ga_id
